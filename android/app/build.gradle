plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    id 'com.google.gms.google-services'
}

android {
    namespace = "com.example.assignment"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion // Consider removing this if not explicitly set

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.assignment"
        minSdk = 23
        targetSdk = 34
        versionCode = 1 // Change this to your desired version code
        versionName = "1.0" // Change this to your desired version name
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            signingConfig = signingConfigs.debug // Consider creating a release signing config
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    // Import the Firebase BoM
    implementation platform('com.google.firebase:firebase-bom:33.3.0')

    // Firebase products you want to use
    implementation 'com.google.firebase:firebase-analytics'
}
